from utils_funcs import sum_number
from utils_funcs import subtraction_number

input_one, input_two = input('Введите число a - '), input('Введите число b - ')

print(sum_number(input_one, input_two))
print(subtraction_number(input_one, input_two))
